kind: Deployment
apiVersion: apps/v1
metadata:
  name: dart-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dart-deployment
  template:
    metadata:
      labels:
        app: dart-deployment
    spec:
      containers:
        - name: dart-deployment
          image: IMAGE_NAME
          imagePullPolicy: Always
          ports:
            - containerPort: 3100
      restartPolicy: Always
---
kind: Service
apiVersion: v1
metadata:
  name: dart-deployment
spec:
  type: NodePort
  ports:
    - port: 3100
      targetPort: 3100
      protocol: TCP
      nodePort: 31000
  selector:
    app: dart-deployment